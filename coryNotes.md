from odoo dev
to get venv going virtual python environment: source venv/bin/activate 
run the server: ./odoo-bin --addons-path="addons,../odoo-tutorials" -d rd-demo
local link: http://localhost:8069/web?debug=assets

to open the SQL python: psql -d rd-demo
\d (table name)
\q is quit
